# Overview & Architecture

> Technical source of truth for the current code. See [handoff.md](./handoff.md) for the planning context and _why_ behind these choices.

## What this is

A portfolio where **the architecture is the portfolio** — a microfrontend system demonstrating cross-framework Module Federation, connecting three independently deployable apps:

- **`naufal-host`** (React) — composes federated components at runtime.
- **`naufal-lab`** (Svelte) — a remote that exposes components over Module Federation.
- **`naufal-party`** (PartyKit) — a WebSocket server powering multiplayer presence.

Federation uses `@module-federation/vite` — the actively maintained, framework-agnostic plugin. (The older `@module-federation/nextjs-mf` is being deprecated and was deliberately not used.)

The remote exposes **two** components today: `./SpringToy` (a draggable Svelte "lanyard ticket" you mount into — and tug out of — the React host) and `./Presence` (a page-wide multiplayer cursor overlay that holds its own live WebSocket). Presence is the on-message demo: a federated Svelte component, embedded in a React host, sharing realtime state across deployments — surfaced as a global, opt-in cursor overlay toggled from the host header.

---

## Project / stack table

| Project             | Stack                                             | Port | Status            |
| ------------------- | ------------------------------------------------- | ---- | ----------------- |
| `naufal-host`       | React 19 + Vite + `@module-federation/vite`       | 5173 | Working           |
| `naufal-lab`        | Plain Svelte 5 + Vite + `@module-federation/vite` | 5174 | Working           |
| `naufal-party`      | PartyKit (WebSocket) — multiplayer presence       | 1999 | Working           |
| `naufal-blog`       | Next.js 16 · static export (standalone, not fed.) | —    | Deployed          |
| `badriyatim-family` | Next.js 16 SSR + Supabase (standalone, not fed.)  | 3000 | WIP, not deployed |

The three federated projects are **separate processes** with no build-time coupling. The host fetches the remote's bundle at runtime from `http://127.0.0.1:5174/remoteEntry.js`; the federated `Presence` component opens a WebSocket to `127.0.0.1:1999`.

The blog (`naufal-blog`) is a standalone static site, **internationalized (EN/ID) with locale-routed URLs** (`/en`, `/id`) for proper multilingual SEO (hreflang + per-locale `sitemap`) — a branded home, the CV page (data + content translated), and an MDX-driven `posts` section (`@next/mdx` + `remark-gfm`, a `[lang]/posts/[slug]` route prerendered per locale × slug, with one `.mdx` body per language). It has a **light/dark/system theme toggle** and a per-post **reading panel** (font family / size / background), both no-FOUC. A root `app/layout.tsx` owns `<html>`/`<body>` above the `[lang]` segment (so a locale switch never re-renders them — keeping the theme class and avoiding a React-19 script warning, see [gotchas.md](./gotchas.md) #24); unmatched URLs use a standard `app/not-found.tsx`. The locale-less section paths (`/cv`, `/posts`) are Cloudflare `_redirects` rules to the default locale, but the apex `/` serves a client stub that **detects the visitor's browser language** and routes to `/en` or `/id` (a static export has no edge to read Accept-Language; no middleware either). It's **deployed at `naufal-blog.pages.dev`** (its own Cloudflare Pages project, independent of the federated three); the first real post — `styling-across-module-federation`, in both locales — is live.

**`badriyatim-family`** is a separate product that shares the repo but nothing else: a private family app (silsilah/events/iuran for Naufal's extended family), deliberately **not** federated and outside the portfolio's design system — it has its own ("Songket & Rumah Gadang", Bahasa-only). It documents itself in `badriyatim-family/CLAUDE.md` + `README.md`; the one repo-level rule worth knowing is its **PII data split**: public pages import only `data/public.json` (names/tree), while the full seed `family.json` is kept **outside the repo entirely** (it goes straight into Supabase; the `data/family.json` gitignore entry remains as a tripwire) — the privacy guarantee is that split, which is what lets this repo be public with the app in it.

---

## What each project contains

### `naufal-host` — React Host (port 5173)

| File                                                                  | Role                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `vite.config.ts`                                                      | Host config; env-aware federation `entry` via `loadEnv`; `theme-prepaint` plugin injects the generated no-FOUC script/style                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `index.html`                                                          | Holds a `<!-- theme-prepaint -->` slot; the Vite plugin swaps in an inline script/style generated from `theme-tokens.ts` (kills FOUC + flash)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `src/lib/theme.ts` / `theme-tokens.ts`                                | Shared theme store (`useSyncExternalStore`, five axes) + the DOM-free token module it shares with the pre-paint generator                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `src/main.tsx`                                                        | Mounts React inside a top-level `ErrorBoundary` + installs an `unhandledrejection` swallower                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `src/App.tsx`                                                         | The home-page playground — header, intro, the Hero block, the eager `IntroSummary` comprehension strip, + lazy-split Chat / TechStack / LiveRemote / Experience / Quality blocks, footer; mounts the global `PresenceOverlay` (+ its pill dock spacer). In canvas mode it swaps the normal `<main>` for the lazy `CanvasStage` around the same block `body`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `src/components/Header.tsx`, `Footer.tsx`                             | Sticky header (name + nav, locale toggle, presence toggle, canvas-view toggle, theme-drawer trigger); footer with social links                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `src/components/Cell.tsx`                                             | The frame primitive: bordered card + monospace label, every block sits in one                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `src/components/RemoteMount.tsx`                                      | Generic wrapper that mounts any framework-agnostic remote (status + `fallback` + `loadingFallback` + `opts`; gates on viewport unless `eager`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `src/components/blocks/HeroBlock.tsx`                                 | Block: host-native interactive wordmark (per-letter cursor repel + brand glow)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `src/components/blocks/TechStackBlock.tsx`                            | Block: host-native rotating icon orbit; pills active on hover/focus/tap                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `src/components/blocks/LiveRemoteBlock.tsx`                           | Block: triggerable federation diagram (you → host → lab) wired to the real `loadRemote`; on success reveals the live `lab/SpringToy`; "Break the connection" exercises the fallback; a real failure offers Retry (cache-busted re-register — [gotchas.md](./gotchas.md) #29)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `src/components/blocks/ExperienceBlock.tsx` + `src/lib/experience.ts` | Block: the career as a `git log --graph` (commit per job with conventional-commit message + hash, framework logos, parallel gigs as real branches, rails draw in on scroll behind a typed `git log` prompt) + detail panel headed by a `git checkout` prompt that retypes per selection and is itself a working terminal (click to type: `checkout` by ref/prefix/`main`, `diff <a> <b>` stack diffs, `log`, `reflog` of the visitor's own session, `status`, `tag` (achievements), `remote -v` (the literal architecture endpoints — the git/MF double meaning), `whoami`, `clear`, `help`, guarded eggs, Tab completion, ↑/↓ history, git-faithful errors) — selecting a commit moves HEAD (pulse + ref pill follow) and lights its rail branch; the panel shows company · industry tag · period · role · stack · summary, a diffstat-style "+ tool" skills delta per job, and a company-site link (`url` in the registry; earlier roles link out too where a domain is known); per-entry `story`/`demo` registry fields light up blog links and inline federated demos as they ship; deep-linkable via `/?exp=<slug>#experience` |
| `src/components/blocks/QualityBlock.tsx` + `scripts/reports.mjs`      | Block: the whole system as a live **architecture map** (host↔lab federation, both → party over WebSocket, blog standalone) — nodes show each app's last published test result, edges carry a subtle always-on current, and clicking a node opens a popover to read what it is, **visit the live app**, and open its full HTML report on the `naufal-reports` Pages site (`VITE_REPORTS_URL`; summary from a `/health.json` seed in dev). Deliberately not a test runner — recorded results, no faked execution. See [deployment.md](./deployment.md) › Quality dashboard                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `src/components/blocks/ChatBlock.tsx` + `src/lib/chat`                | Block: a hand-built **no-LLM assistant** — scores a question against a KB merged from the experience registry (in-app) + the blog's published `knowledge.json` (fetched at runtime via `VITE_BLOG_URL`; degrades to local-only if absent). Intent matching + template NLG + retrieval, bilingual, answers typed out, links route to CV / posts / experience timeline. See [features.md](./features.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `src/components/IntroSummary.tsx`                                     | Eager comprehension strip under the hero (the non-technical rescue): plain-language value-prop + "worked at" companies + CTAs (Ask about me → `#chat`, View CV, Get in touch)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `src/components/PresenceOverlay.tsx`                                  | Global opt-in whole-page cursor overlay — mounts `lab/Presence` (`eager`) when toggled on; rendered at the App root, not in a cell                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `src/components/PresenceToggle.tsx`                                   | Header toggle (cursor icon) for the overlay; reads/writes `lib/presence.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `src/components/ThemeSheet.tsx` · `ThemeControls.tsx`                 | Theme customizer in a header slide-over `Sheet` — mode / accent / surface / radius / font as CSS vars on host `<html>`, re-skins the embedded remote live                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `src/lib/presence.ts`                                                 | Tiny shared store (`useSyncExternalStore`) for the presence on/off state                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `src/components/CanvasStage.tsx` + `src/lib/useCanvas.ts`             | Canvas view (the Figma-mode): the site as a zoom/pan artboard with margin callouts. `useCanvas` is the transform engine (drag-pan, wheel/scroll zoom, touch one-finger pan + two-finger pinch); `CanvasStage` lays out the artboard, measures block centres for the leader-line annotations, and holds the zoom controls. See [features.md](./features.md)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `src/components/CanvasToggle.tsx` + `src/lib/canvas.ts`               | Header toggle (frame icon) + its `useSyncExternalStore` on/off store, mirroring the presence pair                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `src/lib/annotations.ts`                                              | Data-driven canvas-callout registry, keyed to each block's Cell `id` (copy in i18n `canvas.notes.*`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `src/components/ui/sheet.tsx`                                         | shadcn `Sheet` primitive (backs the theme drawer)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `src/lib/mf-fallback-plugin.ts`                                       | MF runtime plugin — `errorLoadRemote` returns a benign stub on init failure, a throwing stub on exposed-module failure (`lab/SpringToy`, `lab/Presence`) (see [mf-platform.md](./mf-platform.md))                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `src/vite-env.d.ts`                                                   | Types the `import.meta.env` vars read in app code: `VITE_LAB_URL` / `VITE_PARTY_HOST` / `VITE_BLOG_URL` / `VITE_REPORTS_URL`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `src/lib/lab-remote.ts`                                               | `LAB_URL` + `ensureLabRemote()` — one-shot runtime `registerRemotes` of the lab, called on first federated use so the remote entry never blocks first paint (see [mf-platform.md](./mf-platform.md))                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `src/types/lab.d.ts`                                                  | Hand-written `lab/*` module declarations — the typed mount contract (see [mf-core.md](./mf-core.md) §5)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |

### `naufal-lab` — Svelte Remote (port 5174)

| File                        | Role                                                                                                                                                                            |
| --------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `vite.config.ts`            | Remote config: `exposes` the mount adapters as `remoteEntry.js`; pins server to `127.0.0.1` (IPv6); `cors` on; `shared: []`                                                     |
| `src/lib/SpringToy.svelte`  | Draggable Svelte "lanyard ticket" (verlet swing physics) — drag it onto the React host to mount, tug to unmount; imports `../app.css` so Tailwind ships in the federated chunk  |
| `src/lib/mountSpringToy.ts` | Mount adapter for `./SpringToy`                                                                                                                                                 |
| `src/lib/Presence.svelte`   | Page-wide multiplayer cursor overlay — opens its own `PartySocket` connection; replays the server's `echoes` as translucent ghost cursors when alone; also imports `../app.css` |
| `src/lib/mountPresence.ts`  | Mount adapter for `./Presence`                                                                                                                                                  |
| `src/App.svelte`            | Standalone page showcasing both exposed components                                                                                                                              |
| `src/app.css`               | Tailwind + shadcn theme — imported by the **Svelte components** (not the mount adapters) so CSS ships over federation                                                           |
| `src/lib/i18n/`             | `svelte-i18n` setup + `en.json` / `id.json`; reads `<html lang>` (host-owned) for locale, observes it for live switches                                                         |

The ticket carries the **Svelte** logo and the host node carries the **React** + **Module Federation** marks, so the boundary reads at a glance — the Svelte thing is literally running inside the React host.

### `naufal-party` — PartyKit Realtime Server (port 1999)

| File            | Role                                                                                                                                                                                                                                                                                                                                                                 |
| --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/server.ts` | Cursor relay: assigns each connection a color + name, broadcasts cursor positions (mouse arrows / touch trails) + tap-firework bursts to the room. Also records visitors' paths into a capped ring buffer of anonymous "ghosts" (seeded) and sends them as `echoes`, so a solo visitor sees translucent traces of recent visitors — see [features.md](./features.md) |
| `partykit.json` | PartyKit config (`name`, `main`)                                                                                                                                                                                                                                                                                                                                     |

---

## Runtime diagram

```text
┌──────────────────────────────────┐   runtime import    ┌──────────────────────────────┐
│  naufal-host  (React, :5173)     │ ──────────────────► │  naufal-lab  (Svelte, :5174) │
│                                  │  lab/SpringToy      │                              │
│  App.tsx                         │  lab/Presence       │ mountSpringToy/mountPresence │
│   ├─ LiveRemoteBlock             │ ◄── (target,opts) ──│   └─ SpringToy / Presence    │
│   │    └─ RemoteMount → SpringToy│      returns cleanup│                              │
│   └─ PresenceOverlay (global)    │                     │  Presence opens ─┐           │
│        └─ RemoteMount → Presence │                     └──────────────────┼───────────┘
└──────────────────────────────────┘                                        │ WebSocket
                                                                            ▼
                                                          ┌───────────────────────────────┐
                                                          │  naufal-party (PartyKit,:1999)│
                                                          │  relays cursor positions      │
                                                          └───────────────────────────────┘
```

---

## Key design decisions

- **Mount-function pattern** over exposing components directly: framework-agnostic, keeps the Svelte runtime out of the host bundle. Reused for every remote regardless of framework. See [mf-core.md](./mf-core.md).
- **Generic `RemoteMount`** over per-remote wrappers: one component handles every remote, plus load status + graceful fallback.
- **`opts` carries context** across the boundary as a plain object — the forward-compatible hook (originally floated for `locale`).
- **Realtime inside a microfrontend**: `Presence` proves a federated remote can own a live WebSocket and share state across deployments. See [features.md](./features.md).
- **Ship CSS with the exposed entry** so Tailwind works embedded (theme via CSS vars, utilities via shipped stylesheet). See [mf-platform.md](./mf-platform.md).
- **No shared Svelte**: `shared: []` — Svelte is fully bundled into the remote. Host shares only `react` / `react-dom`.
- **`build.target: 'chrome89'`** on both sides: required by `@module-federation/vite` (native ES modules + top-level `await`).
- **`127.0.0.1` everywhere**: avoids the Windows IPv6 / dts-plugin IPv4 mismatch. See [gotchas.md](./gotchas.md).
- **React Compiler via a ~12-line custom plugin**, not `@rolldown/plugin-babel`. The documented integration is silently inert in this project (federation + plugin-react v6 interaction); see [features.md](./features.md).
- **i18n is per-remote, locale crosses via `opts.locale`**. Host runs `i18next` + `react-i18next`; each remote will own its own library when translated. Host sets `<html lang>`; remotes will read locale from their mount's `opts`. See [features.md](./features.md).
- **Theming lives on the host, not in any remote**. The theme drawer (a header slide-over `Sheet`) makes the host customizable across five axes (mode, accent, surface, radius, font) — all values on the host's `<html>` (class / `data-*` / CSS vars), backed by one shared store. Because they're CSS variables, they cascade into every remote's mounted DOM with zero coordination code, so re-skinning the page re-skins the federated remote live — a working demo of the cross-boundary cascade. See [features.md](./features.md).
- **Scroll-reveal is plain `IntersectionObserver` + CSS transition**, not Framer Motion (or any animation library). The same hook also backs `RemoteMount`'s optional viewport gate for federated `load()` — though both current remotes opt out with `eager` and load on an explicit user action instead (the live-remote **Run**, the presence toggle). See [features.md](./features.md).
